import { OnDestroy } from '@angular/core';
import { Observable } from 'rxjs/Observable';
export declare abstract class BaseComponent implements OnDestroy {
    unmount$: Observable<void>;
    private unmountEmitter$;
    constructor();
    ngOnDestroy(): void;
}
